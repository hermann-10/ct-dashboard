{
  "buildCommand": "npx ng build --configuration production",
  "outputDirectory": "dist/ct-dashboard/browser",
  "framework": null,
  "rewrites": [
    { "source": "/go/:slug", "destination": "/api/go?slug=:slug" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
